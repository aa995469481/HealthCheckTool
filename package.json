{
  "name": "service-inspection",
  "version": "1.0.0",
  "private": true,
  "description": "业务巡检管理系统（前后端一体）",
  "scripts": {
    "install-all": "npm --prefix server install && npm --prefix web install && npm install",
    "dev": "concurrently -k -n server,web -c blue,green \"npm --prefix server start\" \"npm --prefix web run dev\"",
    "start": "npm run dev"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
